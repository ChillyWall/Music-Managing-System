#ifndef MMS_UI_H
#define MMS_UI_H

#include <utils.h>

int menu_main();

int menu_add(pdb db);

int menu_delete(pdb db);

int menu_search(pdb db);

int menu_filter(pdb db);

int menu_filter_operation(pdb db);

#endif
